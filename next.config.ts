/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['ldgiqdejtmcecnybswit.supabase.co'], // ✅ allow your Supabase project’s domain
  },
};

module.exports = nextConfig;
